let numeros = [3.5, 6.5, 2.0, 0];

let soma = 0;
let quantidade = 0;

for (let i = 0; i < numeros.length; i++) {
  let = numero = numeros[i];

  if(numero === 0);
  break;
}

soma = soma + numero;
quantidade = quantidade + 1;

if(quantidade > 0) {
  let media = soma / quantidade;
  console.log("A média é: " + media);
} else {
  console.log("Nenhum número foi encontrado");
}